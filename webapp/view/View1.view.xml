<mvc:View controllerName="project1.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    displayBlock="true">
    <Page id="page" title="{i18n>title}">
        <content>
            <VBox id="vboxContainer" class="sapUiResponsiveMargin employeeDirectoryShell">
                <Toolbar>
                    <Title text="{i18n>directorySubtitle}" level="H3"/>
                    <ToolbarSpacer/>
                    <ObjectStatus text="{view>/resultCount} of {view>/totalCount} employees" state="Information"/>
                </Toolbar>

                <HBox wrap="Wrap" renderType="Bare" class="sapUiSmallMarginTop sapUiSmallMarginBottom directoryFilters">
                    <SearchField
                        id="searchField"
                        width="22rem"
                        placeholder="Search by name, ID, role, or email"
                        liveChange="onSearch"/>
                    <Select
                        id="departmentSelect"
                        width="14rem"
                        selectedKey="{view>/selectedDepartment}"
                        change="onDepartmentChange"
                        items="{view>/departments}">
                        <items>
                            <core:Item key="{view>key}" text="{view>text}" />
                        </items>
                    </Select>
                </HBox>

                <List id="employeeList" items="{
                        path: 'employees>/EmployeeCollection',
                        sorter: {path: 'name'}
                    }"
                    noDataText="No employees match the current search and filter."
                    updateFinished="onListUpdateFinished">
                    <items>
                        <ObjectListItem
                            id="employeeListItem"
                            title="{employees>name}"
                            number="{employees>id}"
                            numberState="Information"
                            type="Active"
                            press="onEmployeePress"
                            intro="{employees>department}">
                            <attributes>
                                <ObjectAttribute id="roleAttribute" title="Role" text="{employees>role}" />
                                <ObjectAttribute id="emailAttribute" title="Email" text="{employees>email}" />
                                <ObjectAttribute id="locationAttribute" title="Location" text="{employees>location}" />
                                <ObjectAttribute id="managerAttribute" title="Manager" text="{employees>manager}" />
                            </attributes>
                            <firstStatus>
                                <ObjectStatus text="{employees>status}" state="{= ${employees>status} === 'Active' ? 'Success' : 'Warning' }"/>
                            </firstStatus>
                        </ObjectListItem>
                    </items>
                </List>
            </VBox>
        </content>
    </Page>
</mvc:View>
