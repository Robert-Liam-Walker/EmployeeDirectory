<mvc:View controllerName="project1.controller.View2"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:form="sap.ui.layout.form"
    displayBlock="true">
    <Page id="detailPage" title="{i18n>employeeDetailTitle}" showNavButton="true" navButtonPress="onNavBack">
        <content>
            <VBox class="sapUiResponsiveMargin employeeDetailShell" renderType="Bare">
                <ObjectHeader
                    id="objectHeader"
                    title="{employees>name}"
                    number="{employees>id}"
                    numberUnit="{employees>department}">
                    <attributes>
                        <ObjectAttribute id="roleAttributeDetail" title="{i18n>employeeRoleLabel}" text="{employees>role}" />
                        <ObjectAttribute id="emailAttributeDetail" title="{i18n>employeeEmailLabel}" text="{employees>email}" />
                        <ObjectAttribute id="managerAttributeDetail" title="{i18n>employeeManagerLabel}" text="{employees>manager}" />
                    </attributes>
                    <statuses>
                        <ObjectStatus text="{employees>status}" state="{= ${employees>status} === 'Active' ? 'Success' : 'Warning' }" />
                    </statuses>
                </ObjectHeader>

                <form:SimpleForm
                    id="employeeDetailsForm"
                    editable="false"
                    layout="ResponsiveGridLayout"
                    labelSpanXL="3"
                    labelSpanL="3"
                    labelSpanM="4"
                    columnsXL="2"
                    columnsL="2"
                    columnsM="1"
                    class="sapUiMediumMarginTop">
                    <Label text="{i18n>employeeEmailLabel}" />
                    <Link text="{employees>email}" href="{= 'mailto:' + ${employees>email} }" />
                    <Label text="{i18n>employeePhoneLabel}" />
                    <Text text="{employees>phone}" />
                    <Label text="{i18n>employeeDepartmentLabel}" />
                    <Text text="{employees>department}" />
                    <Label text="{i18n>employeeLocationLabel}" />
                    <Text text="{employees>location}" />
                    <Label text="{i18n>employeeManagerLabel}" />
                    <Text text="{employees>manager}" />
                    <Label text="{i18n>employeeHireDateLabel}" />
                    <Text text="{employees>hireDate}" />
                </form:SimpleForm>
            </VBox>
        </content>
    </Page>
</mvc:View>
